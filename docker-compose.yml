services:
  gather-requests:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/home/appuser/data
    environment:
      - DB_PATH=/home/appuser/data/requests.db
      # Uncomment and set these to enable HTTP Basic Auth for /stats/* endpoints
      # - AUTH_USERNAME=admin
      # - AUTH_PASSWORD=changeme
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
